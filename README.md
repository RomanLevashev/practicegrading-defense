# PracticeGrading Defense Materials

Материалы для защиты проекта PracticeGrading.

## Структура

- `sources/` — исходники документов
- `pdf/` — готовые PDF
- `build.sh` — сборка всех документов

## Сборка

```bash
./build.sh
```

После сборки готовые файлы находятся в каталоге `pdf/`.
